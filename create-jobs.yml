- name: create jenkins jobs
  hosts: localhost
  connection: local
  tasks:
#    - name: copy xml file
#      ansible.builtin.copy:
#        src: test.xml
#        dest: /tmp/test.xml

    - name: Create a jenkins job
      community.general.jenkins_job:
       config: "{{ lookup('file', 'test.xml') }}"
       name: test from ansible
       password: admin123
       url: http://52.207.218.232:8080
       user: admin